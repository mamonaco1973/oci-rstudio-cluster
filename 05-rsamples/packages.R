# Example: install gsl into /nfs/rlibs

install.packages("gsl", lib = "/nfs/rlibs")

# Example: install gsl into personal library

# install.packages("gsl")

# Example: install ggplot2 into /nfs/rlibs

# install.packages("ggplot2", lib = "/nfs/rlibs")

# Example: install ggplot2 into personal library

# install.packages("ggplot2")
